# 이번 시간 목표
- 느린 요청으로 인한 영향도
- 메모리를 많이 차지하는 요청으로 인한 영향도
- 대용량 트래픽 상황에서 느린요청이 있다면 스레드를 모두 차지해버릴 가능성이 있다
- 따라서 다른 요청에 영향을 미치게 되고 결국 서비스 가용성이 저해되는 효과가 있다.

- 대용량 트래픽 상황에서 불필요하게 메모리를 많이 사용하는경우
- 특정 API 가 유독 메모리를 낭비하는 경우가 있다면
- 해당 API 들과 동시에 처리되는 API 들은
- 해당 API 들 때문에 오류를 겪을 수 있다.
- OOM

